%tr
  %td~ markdown_to_html(comment.body)
  %td= link_to comment.user.name, comment.user
  %td= link_to 'Destroy', [comment.article, comment], :method => :delete, :data => { :confirm => 'Are you sure?' }

= render partial: 'stream_tabs'


%h1 ストック一覧

%table.table.table-striped.table-bordered
  %thead
    %tr
      %th Title
      %th Author

  %tbody
    - @stocking_articles.each do |article|
      %tr
        %td= link_to article.title, article
        %td= link_to article.author.name, article.author

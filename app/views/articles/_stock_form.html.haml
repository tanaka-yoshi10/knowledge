- if current_user.try!(:stocking?, @article)
  = link_to 'ストック解除', article_stock_url(@article), method: :delete, remote: true, class: 'btn btn-primary'
- else
  = link_to "ストック", article_stock_url(@article), method: :post, remote: true, class: 'btn btn-primary'

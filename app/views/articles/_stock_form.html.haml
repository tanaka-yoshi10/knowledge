-# [review] パーシャルテンプレートの中でインスタンス変数を利用するのはわかりにくいし、使い回しにくくなるので良くないです。
- if current_user.try!(:stocking?, @article)
  = link_to 'ストック解除', article_stock_path(@article), method: :delete, remote: true, class: 'btn btn-primary'
- else
  = link_to "ストック", article_stock_path(@article), method: :post, remote: true, class: 'btn btn-primary'

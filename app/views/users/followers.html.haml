.userPage_ribbon
  #{link_to @user.name, @user} / フォロワー

.tableList
  - @followers.each do |user|
    .tableList_item
      .tableList_avatarCell
        = link_to_user_with_avatar(user, 48)
      .tableList_cell
        = link_to user.name, user
      .tableList_cell

= paginate @followers

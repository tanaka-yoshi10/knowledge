- if user_signed_in?
  - if current_user.following_tag?(tag)
    = link_to "解除", tag_tagfollow_path(tag.name), method: :delete, remote: true, class: 'btn btn-default'
  - else
    = link_to "フォロー", tag_tagfollow_path(tag.name), method: :post, remote: true, class: 'btn btn-primary'
- else
  = link_to "フォロー", tag_tagfollow_path(tag.name), method: :post, class: 'btn btn-primary'

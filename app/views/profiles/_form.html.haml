-# [review] 本来 create で処理すべきところを無理やり update でやるのはレールに乗っていない気がします
= simple_form_for @profile, :url => profile_path, html: { method: :put } do |f|
  = f.input :first_name
  = f.input :last_name
  = f.input :blog_url
  = f.input :organization
  = f.input :introduction, as: :text
  = f.input :qiita_id
  = f.button :submit
